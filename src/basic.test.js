import {expect} from 'chai';

describe('This should pass', () => {
    it('does true equal true', () => {
        // arrange
        const testVal = true;

        // act

        // assert
        expect(testVal).to.equal(true);
    });
});
